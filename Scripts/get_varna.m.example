function [VARNA_DIR, VARNA_JAR] = get_varna()

% make sure to change VARNA_DIR and VARNA_JAR to your local path
% the following line is just a dummy path
% it is used to locate the VARNA applet file

% Give local path, e.g. "/Users/yourname/src/"
% or (less preferred) use "http://" for url, e.g. "https://rmdb.stanford.edu/site_data/VARNA.jar"

VARNA_DIR = '/path/to/VARNA/';
VARNA_JAR = 'VARNA.jar';
